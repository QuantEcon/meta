# Test file for exclusion

This file should be excluded from style checking when using the exclude pattern `.*exclude.*\.md`.

It contains many style violations:
- alpha instead of α
- beta instead of β  
- Bad timing patterns
- Improper headings

But should not be checked when excluded.